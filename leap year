#include <iostream>
using namespace std;
int main(){
    //leap year
    int year;cout<<"Enter a year: ";cin>>year;
    if (year%4==0){
        cout<<"The year is a leap year.";
    }
    else {
        cout<<"The year is not a leap year";
    }
    return 0;
}
