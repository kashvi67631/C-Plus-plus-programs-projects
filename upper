#include <iostream>
#include <cctype>
using namespace std;

int main(){
    string str;cout<<"Enter the string";cin>>str;
    for (int i=0;i<str.length();i++){
        str[i]=toupper(str[i]);
    }
    cout<<str;
    return 0;
}
