#include <iostream>
#include <string.h>
using namespace std;
int main(){
    //reverse the string
    string str;cout<<"Enter the  string:";cin>>str;
    cout<<"\nThe reverse of the string "<<str<<" is ";
    for (int i=str.length();i>=0;i--){
        cout<<str[i];
    }
    return 0;
}
